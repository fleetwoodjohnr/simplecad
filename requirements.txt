# Geometry kernel: OCCT 7.9.3 via the CadQuery OCP bindings.
# Pinned to the exact build verified against this Fedora 44 / Python 3.14 environment.
cadquery-ocp==7.9.3.1.1
# Qt 6 bindings for the shell, viewport host and all UI.
PySide6==6.11.1
# Sketch constraint solver (Levenberg-Marquardt) and linear algebra.
scipy>=1.18
numpy>=2.0
