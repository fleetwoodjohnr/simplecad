"""Geometry kernel.

Importing this package registers every built-in feature type, which is what
lets a saved document be loaded back by name.
"""

from . import (  # noqa: F401
    advanced, arrange, clips, construction, decorative, fasteners, importing,
    operations, primitives, section_replace, sketch_features, split, text, vent,
)
